# Code ownership.
#
# 2026-09-23: FollowUp is a solo build again. Gautam, Vansh, Pransh and
# Dipesh are no longer on the team (founder's decision), so every path
# they owned reverts here. Manoj is a beta tester, not a contributor, and
# has never had repo access.
#
# The old file also pointed every root-level path at
# `@REPLACE-sahil-github-username` — a placeholder that was never filled
# in. GitHub silently ignores an owner that does not exist, so those lines
# were requiring review from nobody. Fixed to the real account here.

* @worksahilca-jpg
