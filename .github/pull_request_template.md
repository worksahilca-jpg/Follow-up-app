## What this changes

<!-- One or two sentences. What does this PR actually do, and why? -->

## Area / owner

<!-- Which area does this touch, per TEAM.md? Frontend (Gautam/Sahil), Backend (Pransh),
     AI/Integrations (Vansh), Security/Ops (Dipesh)? If it crosses areas, say which ones and
     tag the other owner. -->

## How it was tested

<!-- What did you actually run? npx tsc --noEmit / npx eslint / npx vitest run at minimum.
     For UI changes, did you look at it rendered? For trust-guarantee changes (auto-send
     gating, consent, data isolation), what test proves it? -->

## Checklist

- [ ] `npx tsc --noEmit` passes locally
- [ ] `npx eslint` passes locally
- [ ] `npx vitest run` passes locally
- [ ] For UI/design changes: read `design-brain/decisions/rejected.md` and `approved.md` first
- [ ] Flagged any overlap with another owner's area (see `TEAM.md`) in this description
